{
  "description": "Async proto-learning capture, shared by every automated loop in this repo. SubagentStop analyzes each finished subagent's transcript; SessionEnd analyzes the top-level loop session once at the end for loop-level learnings. Both run off the critical path, matching on this ecosystem's shared conventions (github-ops, /goal) rather than naming individual loops, and append a row to the shared Slack canvas when something worth improving happened.",
  "hooks": {
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/capture-proto-learning.sh\" subagent",
            "async": true
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/capture-proto-learning.sh\" orchestrator",
            "async": true
          }
        ]
      }
    ]
  }
}
